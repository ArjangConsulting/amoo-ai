android.useAndroidX=true

# The Compose compiler needs more metaspace than Gradle's default, which otherwise dies with
# "JVM garbage collector is thrashing / out of Metaspace" partway through :composeSampleApp.
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g
